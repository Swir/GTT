# GTT 0.1.35 — Packaged Emergency Patch & Cargo Continuity Evidence

This milestone strengthens the existing Farm Cargo packaged-runtime exercise instead of adding a parallel fake route. Its core recovery proof is the production **emergency patch** followed by deliberate re-breakdown and paid tow.

- The native Mulebox evidence route now stages a real **TowRecommended** mechanical failure while a physical Farm Cargo load is bound to the exact `PersistentVehicleId`.
- The route requests the production emergency roadside patch, pays the real quote, waits for dispatch, and verifies the 30% condition / 32% tire / 5 L limp-home floors without repairing body damage or detached panels.
- Farm Cargo must keep the exact loaded vehicle before and after the patch; its contract timer continues, cargo integrity cannot improve, and the production cargo-recovery subsystem must write both pre-patch and post-patch primary-save checkpoints.
- The route then waits through the real roadside cooldown, deliberately damages the same Mulebox again, requests the existing paid tow, verifies preserved damage and exact identity, rejects a decoy vehicle at Hill Farm, and completes Hill Farm -> North Wood Yard through the real terminals.
- `FARM_CARGO_BREAKDOWN_RUNTIME.json` is upgraded to **schema v2** and binds patch cost, exact identity, body preservation, timer/integrity continuity, real cooldown, tow cost, wrong-vehicle rejection, payout, reputation, authority cleanup and final save to one packaged candidate.
- `DEMO_TECHNICAL_GATE.json` is upgraded to schema 10 and now requires the v2 emergency-patch + re-breakdown/tow evidence before a technical candidate can pass.
- The Win64 evidence workflow now keeps the packaged EXE alive for at least 280 seconds so the strengthened route can finish before evaluation.
- Source sanity is expanded with a dedicated 0.1.35 verifier and playtest matrix.

Roadmap checkbox progress remains **125/130 (96.2%)**. This source-level/runtime-contract milestone **does not prove** a real Unreal Engine 5.8 Win64 package, packaged EXE smoke, Native Chaos acceptance, authored trailer acceptance, or rendered visual approval until the self-hosted Windows runner actually produces that evidence.
