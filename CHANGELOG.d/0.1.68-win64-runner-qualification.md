## GTT 0.1.68 — Win64 UE 5.8 runner qualification

- Added a fast, fail-closed `qualify_win64_runner.ps1` gate for the exact self-hosted Win64/UE 5.8 environment before the expensive package/runtime acceptance begins.
- Qualification binds environment readiness to the exact Git SHA and current ProjectVersion, checks clean tracked state, Git LFS integrity, disk/write capacity and the canonical Win64 preflight.
- Added a time-bounded real `UnrealEditor-Cmd` `-NullRHI` project bootstrap probe so a machine that merely has UE files present cannot falsely qualify.
- Added a standalone manual qualification workflow on the same `self-hosted/windows/x64/unreal-5.8` label contract, with evidence upload on failure for diagnosis.
- Wired the qualification gate ahead of the existing exact-candidate pipeline and kept its evidence alongside the candidate hand-off.
- Added Windows-hosted positive + wrong-version + wrong-SHA fixtures plus a static integration verifier and SWIR/project regressions.
- This is blocker-removal work for the current 0.1.68 candidate; it does not close any ROADMAP checkbox. Progress remains 125/130 (96.2%), human visual review remains required and Demo Release authorization remains false.
