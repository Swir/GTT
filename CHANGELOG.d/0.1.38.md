# GTT 0.1.38 — Packaged Roadside Dispatch + Farm Cargo Continuity Evidence

## Added

- Added an opt-in packaged Farm Cargo dispatch evidence route that exercises the production roadside-service contract on the exact loaded native Mulebox.
- The route proves request-time **locked quote**, **live ETA**, pinned `PersistentVehicleId`, no-charge cancellation for both emergency patch and tow, explicit re-request, exact-charge completion, wrong-vehicle rejection, Hill Farm relay, final delivery, payout, reputation and primary save.
- Added `Scripts/evaluate_farm_cargo_dispatch_runtime.ps1`, producing `FARM_CARGO_DISPATCH_RUNTIME.json` with schema `gtt.farm-cargo-dispatch-runtime.v1` only after all authoritative runtime markers agree.
- Extended the Win64 package evidence chain to keep the packaged executable alive for the later route, evaluate the new manifest and retain it in candidate/failure evidence bundles.
- Added dedicated source-contract CI and a 72-scenario playtest for cancellation, quote/ETA authority, exact-vehicle continuity, economy and release-gate regressions.

## Hardened

- The 0.1.35 verifier now treats later Win64 candidate versions/runtime windows as valid strengthening instead of pinning an obsolete exact workflow default.
- Source evidence still cannot close runtime-only roadmap items: the roadmap remains **125/130 (96.2%)**.
- SWIR Visual Report v3 remains SVG-only; the milestone does not change progress mathematics or release readiness.

## Verification truth

This milestone prepares and source-verifies the runtime contract. It **does not prove** a UE 5.8 Win64 compile/package, packaged-EXE execution, generated `FARM_CARGO_DISPATCH_RUNTIME.json`, Native Chaos/trailer acceptance, rendered visual acceptance or Demo Release readiness. Those gates remain open until a qualifying Windows x64 + UE 5.8 runner executes the exact candidate.
