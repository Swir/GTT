# 0.1.57 — Fieldmaster Native Condition Scale

- Fixed a Native Chaos authority blocker where Fieldmaster condition is a normalized `0..1` ratio but the final drivetrain subsystem compared it against an `8.0` critical threshold and divided it by `100`.
- The critical threshold is now `0.08`, healthy condition feeds the speed governor at its real scale, and drivetrain eligibility can activate for a healthy owned Fieldmaster.
- Native drive telemetry now labels condition explicitly as a percentage while keeping the underlying gameplay state normalized.
- Added a deterministic condition-scale verifier and dedicated CI regression workflow alongside existing native-link and drivetrain checks.
- Roadmap completion intentionally remains `125/130 (96.2%)`: source correction is not a substitute for the required UE 5.8 Win64 compile/package/runtime and visual acceptance evidence.
