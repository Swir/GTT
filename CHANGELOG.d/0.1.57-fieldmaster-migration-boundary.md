# 0.1.57 — Fieldmaster migration-boundary normalization

- Harden the public `ApplyMigrationSnapshot` boundary so canonical native condition is always stored as a `0.0..1.0` ratio while still accepting historical `0..100` ingress defensively.
- Fix Fieldmaster migration diagnostics so human-readable condition output converts the canonical ratio to a real percentage instead of reporting healthy `1.0` as `1.0%`.
- Bound migrated engine and tire upgrade levels to the supported `0..3` range before Native Chaos gameplay consumes them.
- Extend the deterministic condition-scale verifier and packaged witness contract to protect the migration/save roundtrip without claiming the still-missing UE 5.8 Win64 runtime evidence.
- Roadmap completion remains `125/130 (96.2%)`; this reduces a Native Chaos migration blocker but does not close the runtime acceptance checkbox by source evidence alone.
