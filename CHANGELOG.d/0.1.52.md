# GTT 0.1.52 — Packaged Workshop Priority & Pickup Evidence

## Added
- New opt-in `UGTTWorkshopPriorityPickupRuntimeEvidenceSubsystem` for the future deterministic packaged Win64 candidate. It exercises the production STANDARD booking → URGENT promotion → timed check-in → paid checkout → `READY_FOR_PICKUP` → exact fleet release path.
- Runtime proof for deterministic URGENT economics: request-time STANDARD quote, exact +20% locked URGENT quote, no pre-charge, persisted priority and x0.80 workload-derived service duration.
- Runtime proof for one exact locked-quote debit, repaired/refuelled exact vehicle identity, persisted paid pickup checkpoint, pickup hold, wrong-ID pickup rejection and explicit exact-ID release with no second charge.
- `WORKSHOP_PRIORITY_PICKUP_RUNTIME.json` schema `gtt.workshop-priority-pickup-runtime.v1`, evaluator and same-SHA technical-gate promoter from schema 16 to **schema 17**.
- Dedicated 72-case playtest and source-contract verifier for runtime route, persistence, production marker counts, legacy-evidence isolation and Win64 pipeline wiring.

## Changed
- The packaged smoke command now enables the 0.1.52 priority/pickup route after the historical workshop-capacity evidence window.
- The pre-0.1.51 legacy auto-pickup compatibility bridge retains its historical queue/capacity duty but stops before the 0.1.52 explicit pickup window, preventing it from hiding `READY_FOR_PICKUP` before the new proof can inspect it.
- The Win64 candidate workflow now evaluates the new manifest, requires it in success/failure evidence bundles and promotes the technical gate to schema 17 only after the existing schema-16 workshop-capacity gate passes for the same SHA.
- Roadmap progress remains **125 / 130 (96.2%)**. Evidence harness work does not close Native Chaos, authored-trailer, Unreal Engine 5.8 Win64 package/runtime or visual acceptance tasks.

## Verification / limitations
- Linux/source CI can verify code/evidence wiring, quote/timing math contracts, pickup authority boundaries, deterministic SWIR SVG presentation and historical workshop regressions.
- Unreal Engine 5.8 Win64 compilation, packaging, packaged `GTT.exe` smoke execution and rendered visual acceptance are still required before `WORKSHOP_PRIORITY_PICKUP_RUNTIME.json` can truthfully exist as PASS for a release candidate or any public demo Release can be published.
