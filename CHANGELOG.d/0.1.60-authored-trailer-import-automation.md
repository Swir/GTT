# GTT 0.1.60 — Authored Trailer UE Import Automation

## Added
- Added a repeatable Unreal Engine 5.8 headless import path for the project-owned deterministic farm-trailer source rig.
- Enabled the project editor plugins required for unattended Python asset import/save.
- Added an Interchange import script that forces skeletal import, creates/assigns a PhysicsAsset, validates the `body` / `wheel_l` / `wheel_r` hierarchy and normalizes final runtime socket names.
- Added a Windows PowerShell wrapper that regenerates + verifies the glTF source before invoking `UnrealEditor-Cmd`.
- Updated the source-rig contract so hitch/cargo/axle anchors use Unreal Interchange's `SOCKET_` source naming under the skeleton root while retaining the runtime names required by the presentation bridge.
- Added deterministic source verification, dedicated CI and a real-engine acceptance checklist.

## Gate truth
Linux/source CI validates only the automation and deterministic asset contract. This does not claim that UE 5.8 imported the asset on this runner, that a packaged Win64 executable passed smoke/runtime evidence, or that rendered visual acceptance passed. Roadmap truth remains 125/130 (96.2%).
