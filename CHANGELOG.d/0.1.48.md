# GTT 0.1.48 — Packaged Multi-Vehicle Workshop Capacity Evidence

## Added

- Added a deterministic packaged-runtime route that exercises two independent exact-ID workshop appointments against the production 0.1.47 queue.
- The route proves additive on-disk appointment persistence, request-time locked quotes, zero booking/cancellation pre-charge, exact-ID cancellation and deterministic 45-minute service-capacity spacing.
- Added a deliberate constrained-cash execution where the earlier expensive due appointment remains queued while the later affordable due appointment completes exactly once, proving one underfunded vehicle cannot block later capacity.
- Added `WORKSHOP_CAPACITY_RUNTIME.json` (`gtt.workshop-capacity-runtime.v1`) and a schema-15 → schema-16 demo technical-gate promoter that requires same-SHA packaged PASS evidence.
- Added a dedicated source-contract verifier, GitHub Actions workflow and 72-scenario playtest plan covering capacity, persistence, economy, identity and release-gate truth.

## Safety and authority

- Runtime evidence requires two distinct owned native road vehicles and refuses to fake multi-vehicle proof while an active Farm Cargo contract correctly restricts workshop authority to its bound vehicle.
- Hard TOW/IMMOBILE WORKSHOP HOLD remains outside the deferred appointment book through the existing production authority guard.
- The route snapshots and restores both evidence vehicles, player economy and world time, and checks primary Farm Cargo authority fields were not rewound or improved.

## Release truth

Roadmap remains **125 / 130 (96.2%)**. This milestone prepares stronger packaged evidence but does not close any Native Chaos, authored-trailer or Win64/runtime/visual checkbox. `WORKSHOP_CAPACITY_RUNTIME.json` and technical gate schema 16 only become valid when a qualifying UE 5.8 Win64 packaged executable actually runs the route for the exact candidate SHA.
