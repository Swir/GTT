# GTT 0.0.83 — Vehicle & Crime Packaged Demo Route

- Expanded the opt-in packaged demo scenario from core-world presence into a 13-step vehicle/crime route.
- The route now hard-requires live Native Chaos readiness and active legacy takeover for Rusty Fieldmaster 60, Rattleback 82 and Mulebox 1200.
- Added combat-component evidence for the controlled player pawn.
- Added deterministic smoke-only crime injection through the real `UGTTWantedComponent::AddHeat` path; normal player sessions are unchanged.
- Wanted escalation must become observable and a live `AGTTPoliceDirector` must exist before `POLICE_RESPONSE` passes.
- Save remains a real `AGTTGameMode::SaveProgress()` write and is delayed until after the vehicle/crime route has had time to execute.
- `DEMO_SCENARIO.json` advances to schema v2 and records all 13 hard gates plus the deterministic crime action.
- Win64 evidence workflow defaults to 0.0.83 and promotes the vehicle/crime scenario ahead of generic packaged gameplay evaluation.
- Project sanity verifier now enforces the expanded runtime/evaluator/workflow contract.

No roadmap checkbox is closed: this remains source-level preparation until a genuine UE 5.8 Win64 package executes the route and visual acceptance is completed.
