## GTT 0.1.67 — Native Fieldmaster HUD packaged-runtime evidence

- Added `Scripts/evaluate_fieldmaster_hud_runtime.ps1`, a fail-closed packaged-runtime evaluator that maps authoritative Native Fieldmaster telemetry to the exact 0.1.66 driver safety HUD precedence and writes `FIELDMASTER_HUD_RUNTIME.json`.
- The attested Win64 candidate runner now requires the HUD evidence after hill-haul evaluation and before final sealing.
- Exact candidate attestation now hashes and records HUD runtime evidence/counters while keeping `human_visual_review=REQUIRED` and `demo_release_authorized=false`.
- Converted the 0.1.66 HUD source verifier into a forward-compatible regression guard so later milestones cannot fail only because the project version advanced.
- Added deterministic 0.1.67 CI plus packaged playtest instructions.
- The packaged evidence proves authoritative HUD-driving runtime states only; **human visual review** of the exact candidate screenshots remains mandatory before any Demo Release decision.
- This source/evidence milestone does not close any ROADMAP runtime/art gate: the roadmap remains 125/130 (96.2%) until real UE 5.8 Win64/runtime/art evidence exists.
